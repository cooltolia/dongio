include template/header
include template/footer
include ../blocks/**/*
- const alias = "m"

- const pizzaPage = true

doctype html
html(lang='ru')
    +doc-header
    body
        +main-header
        +breadcrumbs(['Пиццы'])
        -const productsList = products.map((item, index) => {item.index = index; return item})
        +catalog('Пиццы', true, productsList)
        +main-footer
        +doc-footer